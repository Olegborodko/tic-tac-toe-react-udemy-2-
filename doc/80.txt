{initialGameBoard.map((row, rowIndex) => <li key={rowIndex}></li>)}

rowIndex не привязан в данном случае к данным, такой вывод не желательный, лучше указывать конкретно уникальное значение id или title или др .. и его можно использовать если элементы не будут меняться местами или удаляться

